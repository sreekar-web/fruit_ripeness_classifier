apiVersion: v1
kind: Service
metadata:
  name: fruit-classifier-service
spec:
  type: NodePort
  selector:
    app: fruit-classifier
  ports:
    - protocol: TCP
      port: 8000
      targetPort: 8000
      nodePort: 30000